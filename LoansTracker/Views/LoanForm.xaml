<Window x:Class="LoansTracker.Views.LoanForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        Title="Create Loan" Height="316" Width="575">
    <Grid Margin="10">
        <StackPanel>
            <ComboBox ItemsSource="{Binding Persons}"
					  SelectedItem="{Binding SelectedPerson, Mode=TwoWay}"
                      DisplayMemberPath="Details"
                      Margin="0,0,0,6"
					  materialDesign:HintAssist.Hint="Select a Person" />

            <xctk:DecimalUpDown
                Value="{Binding Amount, Mode=TwoWay}"
                FormatString="F2"
                Increment="0.01"
                Minimum="0"
                Margin="0,0,0,8" />

            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"  Margin="0,0,0,8" materialDesign:HintAssist.Hint="Description" Cursor="Hand"/>
            
            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                <DatePicker SelectedDate="{Binding Date, UpdateSourceTrigger=PropertyChanged}"
							materialDesign:HintAssist.Hint="Date"
							Margin="0,0,6,0" 
                            Style="{StaticResource MaterialDesignDatePicker}" />

            </StackPanel>
            <Button Content="Save" 
					Command="{Binding SaveCommand}"
					Width="80" Margin="0,4" Cursor="Hand" />
        </StackPanel>
    </Grid>
</Window>