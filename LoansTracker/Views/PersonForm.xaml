<Window x:Class="LoansTracker.Views.PersonForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Create Person" Height="200" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
             Margin="0,0,0,8"
             materialDesign:HintAssist.Hint="Name"
             Style="{StaticResource MaterialDesignFloatingHintTextBox}"
             Cursor="IBeam" />
            <TextBox Text="{Binding Surname, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
             Margin="0,0,0,8"
             materialDesign:HintAssist.Hint="Surname"
             Style="{StaticResource MaterialDesignFloatingHintTextBox}"
             Cursor="IBeam" />
            <Button Content="Save" Command="{Binding SaveCommand}" Width="80" Cursor="Hand" />
        </StackPanel>
    </Grid>
</Window>